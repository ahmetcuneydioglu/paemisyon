/**
 * Geriye dönük uyum: rozetler store-badges.tsx'e taşındı (iOS + Android
 * birlikte). Bu dosya yalnız eski içe aktarımları kırmamak için duruyor.
 */
export { AppStoreBadge, PlayStoreBadge, StoreBadges } from "./store-badges";
